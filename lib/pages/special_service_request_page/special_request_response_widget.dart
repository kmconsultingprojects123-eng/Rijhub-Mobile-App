import 'package:flutter/material.dart';

// Removed: This page was a simulated response detail view. Use the
// Special Requests history page for viewing responses. This stub avoids
// missing imports while the app is refactored.

class SpecialRequestResponseWidget extends StatelessWidget {
  const SpecialRequestResponseWidget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Removed')),
      body: const Center(child: Text('This widget has been removed. See Special Requests history.')),
    );
  }
}

